{
  "name": "gulliver-guide",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered weekend travel platform for the DACH market — plan train-based weekend trips across Germany",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:client": "cd client && npm install --include=dev --legacy-peer-deps",
    "install:server": "cd server && npm install --include=dev",
    "postinstall": "npm run install:client && npm run install:server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "node server/dist/index.js",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "keywords": ["travel", "germany", "weekend-trip", "ai", "train", "bahn"],
  "license": "MIT"
}
